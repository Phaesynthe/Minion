/* global module */

module.exports = {
  routeHandlers: true,
  swagger: true, // ToDo: support
  tests: true, // ToDo: support
  postman: true, // ToDo: support
  requests: true, // ToDo: support
  mocks: true // ToDo: support
}
